Imports System.Net.Mail
Public Class Mailer
    Dim cust As New Customer
    Dim prop As New Properties
    Dim server As String = ""


    Public Sub MakeOffer(OwnerMail As String, BuyerMail As String, prop As Properties)
        'TODO Mailer.MakeOffer
    End Sub
    Sub notifyNewProperty()
        'TODO Mailer.notifyNewProperty
    End Sub
End Class
